# Requirements for VODER

# GUI Framework
PyQt5>=5.15.0

# Core Audio Processing
numpy>=1.21.0
scipy>=1.10.0
soundfile>=0.12.0
sounddevice>=0.5.0
pydub>=0.25.0

# PyTorch (conservative version for compatibility)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# ML/AI Models
openai-whisper>=20231117
transformers>=4.30.0
accelerate>=0.20.0
huggingface-hub>=0.16.0
einops>=0.6.0

# Configuration
omegaconf>=2.3.0
hydra-core>=1.3.0
PyYAML>=6.0

# TTS and more ofc
qwen-tts==0.1.1

# Voice Conversion & Music Generation
munch>=4.0.0
gradio>=4.0.0
funasr>=1.0.0
modelscope>=1.9.0
librosa>=0.10.0

# Note: For optimal performance on different platforms:
# - Linux: Install PyTorch with CUDA support separately if needed
# - macOS: PyTorch with MPS support available for Apple Silicon
# - Windows: PyTorch with CUDA support available
# - Important: if you got "module not found" errors, just pip install them and you are ok
